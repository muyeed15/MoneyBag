'use client'
import { useActionState } from 'react'; import useSWR from 'swr'; import { FileText } from 'lucide-react'; import { generateStatementAction } from '@/app/actions'; import { Button } from '@/components/ui/Button'; import { PageHeader } from '@/components/ui/PageHeader'; import type { AccountStatement, PaginatedResponse } from '@/types'; import { formatAmount, formatDate } from '@/utils/helpers'
const initial={ok:false,message:''}
export default function StatementsPage(){const[state,action,pending]=useActionState(generateStatementAction,initial);const{data}=useSWR<PaginatedResponse<AccountStatement>>('/api/statements?page=1');const now=new Date();return <div><PageHeader title="Statements" subtitle="Monthly Summary" showBack/><div className="px-4 py-5 lg:px-8 lg:py-8 mx-auto max-w-2xl space-y-5"><form action={action} className="bg-white border border-sage-mid rounded-2xl p-5"><p className="font-semibold text-navy mb-4">Generate a statement</p><div className="grid grid-cols-2 gap-3"><label className="text-[11px] font-semibold uppercase tracking-widest text-navy-muted">Month<select name="month" defaultValue={now.getMonth()+1} className="mt-2 w-full border border-sage-mid rounded-xl p-3 text-sm bg-white">{Array.from({length:12},(_,i)=><option key={i} value={i+1}>{new Date(2000,i).toLocaleString('en',{month:'long'})}</option>)}</select></label><label className="text-[11px] font-semibold uppercase tracking-widest text-navy-muted">Year<input name="year" type="number" defaultValue={now.getFullYear()} className="mt-2 w-full border border-sage-mid rounded-xl p-3 text-sm"/></label></div>{state.message&&<p className={`text-sm mt-3 ${state.ok?'text-teal':'text-red-600'}`}>{state.message}</p>}<Button className="w-full mt-4" loading={pending}>Generate Statement</Button></form><div className="space-y-3">{data?.results.map(s=><div key={s.id} className="bg-white border border-sage-mid rounded-2xl p-5"><div className="flex justify-between items-start"><div className="flex gap-3"><div className="h-10 w-10 bg-teal/10 rounded-xl grid place-items-center"><FileText className="h-5 w-5 text-teal"/></div><div><p className="font-semibold text-navy">{s.period}</p><p className="text-xs text-navy-muted">{s.transaction_count} transactions · {formatDate(s.generated_at)}</p></div></div><p className="font-bold text-navy">{formatAmount(s.closing_balance)}</p></div><div className="grid grid-cols-3 gap-3 mt-5 pt-4 border-t border-sage-mid text-xs"><div><p className="text-navy-muted">Opening</p><p className="font-semibold text-navy">{formatAmount(s.opening_balance)}</p></div><div><p className="text-navy-muted">Credits</p><p className="font-semibold text-teal">+{formatAmount(s.total_credits)}</p></div><div><p className="text-navy-muted">Debits</p><p className="font-semibold text-navy">-{formatAmount(s.total_debits)}</p></div></div></div>)}{!data?.results.length&&<p className="bg-white border border-sage-mid rounded-xl py-14 text-center text-sm text-navy-muted">No statements generated yet.</p>}</div></div></div>}
